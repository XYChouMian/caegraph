# Tutorials

No tutorials yet — CAEGraph is in Phase 1 (Core Data Structures).

Planned tutorial series (will follow implementation phases):

1. Loading CAE data into a `Mesh`
2. Converting a `Mesh` to a PyG `Graph`
3. Building a `CAEDataset` with transforms and splits
4. Training your first GNN surrogate model
5. Adding physics-informed losses
