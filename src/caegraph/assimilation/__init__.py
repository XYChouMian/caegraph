"""Experimental-data assimilation of CAEGraph.

Planned (ADR-008 R4, Phase 3): observation operators (sparse
measurements, e.g. PIV) and correction operators (dense prediction +
observation -> improved field). Consumed in two modes: by workflow
(training constraint) and by inference (post-prediction correction).

No implementation yet.
"""
