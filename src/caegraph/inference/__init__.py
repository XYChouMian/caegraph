"""Neural-simulation harness of CAEGraph.

Planned (ADR-008 R3, Phase 4): simulator (mesh + pretrained model ->
fields) and the transient rollout loop harness. Numerical time-stepping
schemes stay model-side (ADR-007 D5).

No implementation yet.
"""
