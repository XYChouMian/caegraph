name: Test
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install
        run: pip install .
      - name: Test
        run: pytest
      - name: Documentation
        run: mkdocs build
